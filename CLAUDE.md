# CLAUDE.md

Jekyll + Chirpy 기반 개인 기술 블로그 `harley's dev note`. 이 파일은 이 저장소에서 글을 쓰거나 고칠 때 지켜야 하는 규약을 정리한 것이다.

## 저장소 구조

- `_posts/` — 모든 글. 파일명은 `YYYY-MM-DD-<slug>.md`
- `_includes/series-nav.html` — 시리즈 내비게이션. Chirpy 원본에 없는 자체 추가
- `_layouts/post.html` — Chirpy 7.6.0 오버라이드. 변경점은 series-nav 인클루드와 AdSense 슬롯 3곳뿐이다
- `_plugins/posts-lastmod-hook.rb` — git log 에서 `last_modified_at` 을 유도한다. **프론트매터에 수정일을 직접 쓰지 않는다**
- `assets/img/posts/<slug>/` — 글별 그림
- `assets/lib/` — Chirpy 정적 자산 서브모듈. 사이트 빌드에서 제외돼 있다. 건드리지 않는다
- `docs/setup-checklist.md` — 발행되지 않는 대시보드 설정 체크리스트

## 프론트매터

키 순서를 지킨다.

```yaml
---
title: "(Radar) 9. 표적 추적 — 검출 목록을 트랙으로 잇기"
description: "프레임마다 나오는 검출 목록을 어떻게 하나의 표적으로 잇는가 — 극좌표가 만드는 찌그러진 오차 타원, 칼만 필터와 α-β, 게이팅과 연관, IMM, 트랙 관리, 그리고 필터가 제대로 튜닝됐는지 숫자로 확인하는 방법까지."
date: 2026-08-02 10:00:00 +0900
slug: 'radar-tracking'
categories: [Dev, Radar]
series: radar
series_order: 9
tags: [radar, tracking, kalman-filter, imm, data-association, jpda, alpha-beta, signal-processing, radar-sensor]
math: true
---
```

| 키 | 필수 | 규칙 |
|---|---|---|
| `title` | O | `"(접두어) N. 제목"`. 개념 글은 ` — 부제` 를 덧붙인다. N은 `series_order` 와 일치시킨다. 접두어는 `(Radar)`, `(C#)`, `(WPF)`, `(OpenCV C++)` |
| `description` | O | 무엇을 다루는지 열거하는 한 문장. `— ... 까지.` 형태가 기본이다. SEO 메타와 피드에 그대로 나가므로 제목을 되풀이하지 않는다 |
| `date` | O | `YYYY-MM-DD 10:00:00 +0900` |
| `slug` | O | 파일명의 slug 와 같은 값. 인용 부호로 감싼다 |
| `categories` | O | 2단 이하. `[Dev, X]` / `[Algorithm, X]` / `[Project]` / `[ETC]` |
| `tags` | O | 소문자 하이픈. 시리즈 공통 태그를 앞에, 글 고유 태그를 뒤에 둔다 |
| `series`, `series_order` | 시리즈 글만 | 아래 「시리즈 규약」 참조 |
| `math` | 수식 있을 때만 | `true`. 수식이 없으면 키를 넣지 않는다 |
| `toc` | 선택 | 허브 글과 아주 짧은 글에만 `false` |

`cnn` 과 `docker` 시리즈의 제목은 위 번호 규칙보다 앞서 쓰인 것이다. 요청받지 않았다면 소급해 바꾸지 않는다.

## 시리즈 규약

`_includes/series-nav.html` 의 계약이다.

- `series: <키>` 와 `series_order: <번호>` 가 함께 있으면 이전 장·다음 장 링크가 자동 생성된다
- 현재 키: `radar`, `csharp`, `opencv`, `wpf`, `cnn`, `docker`
- 허브(목차) 글은 `series_hub: true`, `series_order: -1`, `pin: true`, `toc: false` 를 쓴다. 이전/다음 체인에서 제외된다
- **새 장을 추가하면 허브 글의 목차 목록도 같이 갱신한다.** 목차 본문은 자동 생성이 아니다
- 아직 쓰지 않은 장은 허브에 `N. 제목 — 작성 예정` 으로 자리를 남긴다

## 본문 구조

현행 기준은 radar 시리즈 6~10장이다.

1. **도입** — 제목 없이 `이번 장에서는 ~를 다루겠다` 로 주제를 먼저 밝히고 시작한다. `앞 장에서 ~를 미뤄 두었다`, `이번 장이 그 ~다` 같은 앞 장 회고로 시작하지 않는다
1-1. **다른 장 이야기를 하지 않는다** — `다음 장부터 ~로 들어가겠다`, `~는 뒤의 장에서 다루겠다`, `N장에서 본 대로`, `N장 M절에서 ~라고 한 것이 이것이다` 같은 문장은 전부 뺀다. 독자는 목차를 볼 수 있다. 다른 장의 내용이 필요하면 그 사실만 쓰고, 유도나 근거의 위치는 `그 유도는 [7장](/posts/antenna-basics/) 3절에 있다` 처럼 링크로만 건다. 같은 글 안의 절 참조(`5절에서 본 대로`)는 괜찮다
2. **기준 제원** — 여러 절이 같은 값을 반복 참조하는 예산 계산형 글(2·3·6장)만 `출처` 열이 있는 표로 고정한다. 개념 설명형 글(7장 이후)은 제원을 따로 선언하지 않고, 값이 처음 쓰이는 자리에서 문장 안에 넣는다. `계산은 지금까지처럼 ~ 제원을 그대로 쓴다` 같은 안내 문장은 넣지 않는다
3. **`## N. 제목`** — h2에는 번호를 붙이고, `### 제목` h3에는 붙이지 않는다
4. **`## N. 정리`** — 마지막 번호 절
5. **`## 참고 자료`** — 번호 없음. `저자, *제목*, 출판사 — 이 자료에서 무엇을 참조했는지` 형식으로 쓴다

절 사이는 `* * *` 로 나눈다.

### 문체

- **평서형 "~다"** 를 쓴다. "~습니다" 를 쓰지 않는다
- 굵게(`**`)는 문장의 결론이나 실무에서 중요한 단정에만 쓴다
- 용어는 처음 나올 때 한국어와 원어를 함께 준다: `연관(association)`
- **번역어가 오히려 낯선 용어는 원어를 앞에 둔다.** `격자엽(grating lobe)` 보다 `grating lobe(격자엽)` 가 낫다. 현장에서 원어로 부르는 말은 원어가 먼저 눈에 들어와야 한다
- **글이 자기 문장을 두고 논평하지 않는다.** `문장은 짧은데`, `말이 어렵지만`, `한 번 짚어야 한다`, `이름이 거창하지만` 같은 절은 내용이 아니라 글쓴이의 혼잣말이다. 설명할 것이면 그냥 설명한다
- **개념을 먼저 말하고 숫자는 뒤에 둔다.** 절이나 문단을 계산값으로 열지 않는다. 무엇을 하는 것인지 평범한 말로 정의하고, 왜 그것이 문제인지 말한 다음, 근거로 숫자를 붙인다
- **굳은 용어는 평범한 말로 먼저 말하고 이름을 뒤에 붙인다.** `결정적 빔포밍은 ~` 보다 `미리 정해 두는 쪽을 결정적(deterministic) 빔포밍이라 한다` 가 낫다. `개구`, `급전망`, `부배열`, `준위`, `자유도`, `첨두`, `포화` 처럼 업계에서만 쓰는 말은 처음 나올 때 한 구절로 풀어 준다
- 화살표 연쇄(`A → B → 실패`), 즉석에서 만든 약어, 하이픈 적층 복합어를 쓰지 않는다. 완전한 문장으로 쓴다. 6개월 뒤 다시 읽을 사람이 독자다
- **개발 일지의 말투로 쓴다.** 글쓴이가 자기 작업을 적어 두는 자리다. `이 장은 ~한다`, `이 시리즈는 ~한다` 같은 비인칭 서술 대신 `이번에는 ~해 보겠다`, `~정리해 두었다`, `여기서는 ~만 보겠다`로 쓴다.
- **개념을 비유 명사로 부르지 않는다.** `관문`, `무대`, `손잡이`, `매듭`, `벽`, `문`, `얼굴`, `~의 언어로는` 같은 표현 대신 그 대상의 이름을 그대로 쓴다
- **경구체를 쓰지 않는다.** 아래는 전부 피한다
  - 대구와 반전으로 다듬은 제목: `무엇을 사고 무엇을 파는가`, `빔이 만들어지는 자리`, `X 하나가 Y 를 가른다`
  - 거래 비유: `사다`, `팔다`, `치르다`, `지불하다`, `대가`, `값을 낸다`. 그냥 무엇이 늘고 무엇이 줄어드는지 쓴다
  - 단정 어미 상투구: `~의 본질이다`, `~인 셈이다`, `~가 전부다`, `결국 ~란`
  - 절 제목은 그 절이 다루는 대상을 그대로 쓴다. `### 스윕 선형성`이 `### 램프가 휘면 먼 표적부터 무너진다`보다 낫다
  - 문장 중간의 ` — ` 삽입구는 문서당 서너 번을 넘기지 않는다. 마침표로 끊는 편이 낫다
  - 마무리 절에 경구를 번호로 나열하지 않는다. 뒤에서 실제로 다시 쓰게 될 것만 평범한 문장으로 적는다
- 굵게는 글 하나에 스무 곳을 넘기지 않는다. 스무 곳을 넘으면 강조가 아니라 배경이 된다. 목록 항목 앞머리에 놓는 용어 이름표(`- **성긴 배열** — ...`)는 강조가 아니라 구조라서 이 수에 세지 않는다

### 수치 정책

**본문에 적는 숫자는 실제로 계산해 검산한 값이어야 한다.** 그림도 실제로 돌려서 뽑은 결과여야 한다. 검산하지 않은 값을 그럴듯하게 적지 않는다. 다만 검산했다는 사실 자체를 본문에 선언하지 않는다. 계산할 수 없거나 근사라면 어떤 조건에서 성립하는 근사인지 본문에 밝힌다.

### 그림

```markdown
![추적 파이프라인](/assets/img/posts/radar-tracking/fig1-tracking-pipeline.svg){: width="860"}
_그림 1. 되먹임 고리가 핵심이다. 예측이 게이트를 만들고, 게이트가 연관을 좁히고, 연관이 다시 예측을 고친다._
```

- 경로는 `/assets/img/posts/<slug>/fig<N>-<이름>.svg`. SVG 를 기본으로 한다
- 폭은 `{: width="860" }`
- **SVG 루트에 `width` 와 `height` 를 반드시 넣는다.** `viewBox` 만 있으면 브라우저가 고유 크기를 300px 로 잡고, Chirpy 의 GLightbox 는 그 값으로 팝업 크기를 정하므로 그림을 클릭했을 때 아주 작게 뜬다. 설계 크기(860 × H)의 3배를 넣어 팝업이 화면을 채우게 한다. 본문 렌더링은 `width="860"` 이 잡으므로 영향이 없다
- 캡션은 `_그림 N. ..._` 한 줄. 그림에서 무엇을 읽어야 하는지를 쓴다. 제목을 반복하는 자리가 아니다
- **개념을 설명하는 그림에는 수치를 적지 않는다.** 각도나 dB 를 그림 위에 써 두면 독자가 그림이 아니라 숫자를 읽는다. 그림은 크다·작다, 들어온다·막힌다를 보여 주는 자리고 값은 본문 표로 준다. 다만 그림에 그린 것은 실제로 계산한 결과여야 한다
- **원리를 말로만 설명하고 있으면 그림을 하나 넣는다.** 겹치고 지워지는 파형, 옆에서 들어오는 간섭처럼 그려 보면 한 번에 보이는 것은 문단을 늘리는 대신 그린다. 그림을 중간에 끼워 넣으면 뒤 그림의 번호와 파일명을 전부 다시 매긴다

### 수식

인라인은 `$...$`, 블록은 `$$...$$`. 프론트매터에 `math: true` 가 있어야 렌더링된다.

### 코드

코드펜스 언어는 `c`, `cpp`, `python`, `csharp`, `bash`, `xml`, `text` 를 쓴다. C 코드에는 `cpp` 가 아니라 `c` 를 쓴다. 옛 글에 `c++`, `c#` 이 남아 있지만 새 글에서는 쓰지 않는다.

### 강조 블록

Chirpy 프롬프트 블록은 두 종류만 쓴다.

```markdown
> 24 GHz 에서 사람이 1 m/s 로 걸어오면 도플러는 160 Hz 다.
{: .prompt-tip }

> 이 근사는 $f\tau \ll 1$ 에서만 성립한다.
{: .prompt-warning }
```

프롬프트 블록은 독자에게 주는 내용(예시 수치, 주의점)에만 쓴다. "이 글의 숫자는 검산한 값이다" 같은 검산 선언은 쓰지 않는다 — 검산은 아래 「수치 정책」의 작업 규율이지 본문에 적는 문구가 아니다.

## 로컬 빌드·검증

```shell
bundle install
bash tools/run.sh      # jekyll serve -l, 127.0.0.1
bash tools/test.sh     # production 빌드 + htmlproofer
```

글을 추가했거나 링크·그림 경로를 고쳤으면 `tools/test.sh` 를 돌린다. htmlproofer 가 끊어진 내부 링크와 없는 이미지를 잡아 준다.

## 작업 원칙

- **커밋과 푸시는 하지 않는다.** 저자가 직접 한다. 변경은 작업 트리에 남겨 두고 무엇을 어떤 파일에서 바꿨는지 보고한다. 브랜치 생성, `reset`, `rebase`, `amend` 처럼 히스토리를 건드리는 조작도 명시적으로 요청받았을 때만 한다
- **길이는 내용이 요구하는 만큼으로 맞춘다.** 본질은 다 다루되, 채우기용 절·중복 요약·보일러플레이트로 문서를 늘리지 않는다
- **요청받은 범위만 한다.** 애매한 부분은 통상적 판단으로 처리하고, 해석에 따라 결과물이 크게 달라질 때만 묻는다. 요청하지 않은 절을 추가하거나 기존 글의 구조를 조용히 재편하지 않는다
- **검증하라는 지시는 필요 없다.** 스스로 확인한다. 다만 숫자는 위 「수치 정책」대로 실제 계산으로 확인한다
- 발행된 글의 문체를 손볼 때는 이 파일의 문체 규칙을 기준으로 삼는다
